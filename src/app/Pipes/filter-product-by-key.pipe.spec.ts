/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterProductByKeyPipe } from './filter-product-by-key.pipe';

describe('FilterProductByKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProductByKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
